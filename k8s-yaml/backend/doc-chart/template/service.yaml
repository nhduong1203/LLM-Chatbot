apiVersion: v1
kind: Service
metadata:
  name: doc-management-api
spec:
  selector:
    app: doc-management-api
  ports:
  - port: {{ .Values.port }}
    targetPort: {{ .Values.port }}
  type: ClusterIP
